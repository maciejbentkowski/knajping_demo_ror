<div class="grid grid-cols-9 mt-5 border p-8 w-full  bg-white shadow-md rounded-lg dark:bg-surface-dark min-h-64">
  <div class="col-start-2 col-span-7 pl-4 py-4 rounded">
    <%= form_with model:[venue, review] do |form| %>
      <div>
        <div class>
          <%= form.label :title, "Tytuł:" %>
        </div>
        <div class="mt-3">
          <%= form.text_field :title, class: "w-full" %>
        </div>
      </div>
      <div class="mt-4">
        <div>
          <%= form.label :content, "Opis:" %>
        </div>
        <div class="mt-3">
          <%= form.text_area :content, class: "w-full h-3/5" %>
        </div>
      </div>
      <div class="my-9">
        <%= form.fields_for :rating do |ratings_subform| %>
          <div class="my-3 py-4 border shadow ">
            <div class="text-center pt-2 text-2xl">
              <%= ratings_subform.label :value_rating, "Oceń wartość" %>
            </div>
            <div class="text-xs px-5 mb-8 text-center">
              Czy jedzenie jest dobrze przygotowane, świeże i smaczne? Jak wyglądają dania? Czy oferta jest zróżnicowana, dostosowana do różnych preferencji dietetycznych?
            </div>
            <div class="text-center flex mt-1 mb-3 justify-around">
              <% (1..6).each do |rating_value| %>
                <label>
                  <%= ratings_subform.radio_button :value_rating, rating_value %>
                  <%= rating_value %>
                </label>
              <% end %>
            </div>
          </div>
          <div class="my-3 py-4 border shadow">
            <div class="text-center pt-2 text-2xl">
              <%= ratings_subform.label :service_rating, "Oceń serwis" %>
            </div>
            <div class="text-xs px-5 mb-8 text-center">
              Czy obsługa jest uprzejma, pomocna i kompetentna? Czy czas oczekiwania jest adekwatny do rodzaju potraw?
            </div>
            <div class="text-center flex mt-1 mb-3 justify-around">
              <% (1..6).each do |rating_value| %>
                <label>
                  <%= ratings_subform.radio_button :service_rating, rating_value %>
                  <%= rating_value %>
                </label>
              <% end %>
            </div>
          </div>
          <div class="my-3 py-4 border shadow">
            <div class="text-center pt-2 text-2xl">
              <%= ratings_subform.label :atmosphere_rating, "Oceń atmosferę" %>
            </div>
            <div class="text-xs px-5 mb-8 text-center">
              Czy restauracja jest estetyczna, wygodna i przytulna? Czy muzyka i światło są odpowiednie, tworząc przyjemny nastrój?
            </div>
            <div class="text-center flex mt-1 mb-3 justify-around">
              <% (1..6).each do |rating_value| %>
                <label>
                  <%= ratings_subform.radio_button :atmosphere_rating, rating_value %>
                  <%= rating_value %>
                </label>
              <% end %>
            </div>
          </div>
          <div class="my-3 py-4 border shadow">
            <div class="text-center pt-2 text-2xl">
              <%= ratings_subform.label :quality_rating, "Oceń jakość" %>
            </div>
            <div class="text-xs px-5 mb-8 text-center">
              Czy ceny są odpowiednie do jakości jedzenia i obsługi? Czy lokal i jego otoczenie są zadbane, czyste i schludne?
            </div>
            <div class="text-center flex mt-1 mb-3 justify-around">
              <% (1..6).each do |rating_value| %>
                <label>
                  <%= ratings_subform.radio_button :quality_rating, rating_value %>
                  <%= rating_value %>
                </label>
              <% end %>
            </div>
          </div>
          <div class="my-3 py-4 border shadow">
            <div class="text-center pt-2 text-2xl">
              <%= ratings_subform.label :availability_rating, "Oceń dostępność" %>
            </div>
            <div class="text-xs px-5 mb-8 text-center">
              Czy restauracja znajduje się w dogodnym miejscu? Czy restauracja jest otwarta w dogodnych dla klientów godzinach?
            </div>
            <div class="text-center flex mt-1 mb-3 justify-around">
              <% (1..6).each do |rating_value| %>
                <label>
                  <%= ratings_subform.radio_button :availability_rating, rating_value %>
                  <%= rating_value %>
                </label>
              <% end %>
            </div>
          </div>
          <div class="my-3 py-4 border shadow">
            <div class="text-center pt-2 text-2xl">
              <%= ratings_subform.label :uniqueness_rating, "Oceń unikalność" %>
            </div>
            <div class="text-xs px-5 mb-8 text-center">
              Czy restauracja oferuje oryginalne dania lub unikalne połączenia smakowe? Czy oferowanie tradycyjne dania są wyjątkowe w swoim smaku?
            </div>
            <div class="text-center flex mt-1 mb-3 justify-around">
              <% (1..6).each do |rating_value| %>
                <label>
                  <%= ratings_subform.radio_button :uniqueness_rating, rating_value %>
                  <%= rating_value %>
                </label>
              <% end %>
            </div>
          </div>
        <% end %>
      </div>
      <div class="border shadow my-3 px-6 py-2 bg-white text-indigo-600 rounded-lg font-semibold hover:bg-indigo-50 text-center ">
        <%= form.submit %>
      </div>
    <% end %>
  </div>
</div>
